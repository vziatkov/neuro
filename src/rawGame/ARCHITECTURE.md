# Архитектура NeuroKids Game

## 📁 Структура проекта

```
src/rawGame/
├── components/          # UI компоненты
│   ├── MenuScreen.tsx   # Экран главного меню
│   ├── GameScreen.tsx   # Игровой экран (контейнер)
│   ├── GameHeader.tsx   # HUD с прогрессом
│   ├── VisualAid.tsx    # Визуальная помощь (Ten-Frame)
│   ├── ProblemDisplay.tsx # Отображение задачи
│   ├── NumberPad.tsx     # Цифровая клавиатура
│   ├── FeedbackOverlay.tsx # Обратная связь
│   └── Visualizer.tsx    # Ten-Frame визуализация
├── hooks/               # Custom React hooks
│   └── useGameLogic.ts  # Вся игровая логика
├── services/            # Бизнес-логика
│   └── NeuroMathAI.ts   # AI адаптация сложности
├── types.ts             # TypeScript типы
├── constants.ts         # Константы конфигурации
├── App.tsx              # Главный компонент (роутинг)
└── index.tsx            # Entry point
```

---

## 🎯 Принципы архитектуры

### 1. **Разделение ответственности (Separation of Concerns)**

- **Components** — только UI, без бизнес-логики
- **Hooks** — управление состоянием и побочными эффектами
- **Services** — чистая бизнес-логика (AI, алгоритмы)
- **Constants** — конфигурация в одном месте

### 2. **Композиция компонентов**

Вместо одного монолитного компонента:
- `MenuScreen` — отдельный экран меню
- `GameScreen` — контейнер для игрового экрана
- `GameHeader`, `VisualAid`, `ProblemDisplay`, `NumberPad` — переиспользуемые компоненты

### 3. **Custom Hooks для логики**

Вся игровая логика вынесена в `useGameLogic`:
- Управление состоянием
- Обработка ввода
- AI взаимодействие
- Таймеры и эффекты

### 4. **Централизованные константы**

Все магические числа вынесены в `constants.ts`:
- Легко настраивать
- Легко тестировать
- Легко документировать

---

## 🔄 Поток данных

```
App.tsx
  └─> useGameLogic (hook)
       ├─> NeuroMathAI (service)
       ├─> State management
       └─> Event handlers
  └─> MenuScreen / GameScreen
       └─> Child components
```

### Пример потока:

1. **Пользователь нажимает кнопку** → `NumberPad` вызывает `onInput`
2. **`useGameLogic` обрабатывает** → обновляет `userAnswer`
3. **Пользователь нажимает GO** → `submitAnswer` в `useGameLogic`
4. **AI обрабатывает результат** → `NeuroMathAI.processResult`
5. **Обновляется профиль** → `setProfile` в `useGameLogic`
6. **Показывается feedback** → `FeedbackOverlay`
7. **Через 2 секунды** → `nextProblem` (через `useEffect`)

---

## ✨ Улучшения

### 1. **Исправлен глобальный экземпляр AI**

**Было:**
```typescript
const ai = new NeuroMathAI(); // ❌ Вне компонента
```

**Стало:**
```typescript
const ai = useRef(new NeuroMathAI()).current; // ✅ Внутри hook
```

### 2. **Исправлены утечки памяти**

**Было:**
```typescript
setTimeout(() => nextProblem(), 2000); // ❌ Нет очистки
```

**Стало:**
```typescript
useEffect(() => {
  if (!feedback.type) return;
  const timer = setTimeout(() => nextProblem(), GAME_CONFIG.FEEDBACK_DELAY_MS);
  return () => clearTimeout(timer); // ✅ Очистка
}, [feedback.type, nextProblem]);
```

### 3. **Вынесены константы**

**Было:**
```typescript
if (result.timeTaken < 5) speedBonus = 5; // ❌ Магические числа
```

**Стало:**
```typescript
if (result.timeTaken < AI_CONFIG.SPEED_THRESHOLDS.FAST_SECONDS) {
  speedBonus = AI_CONFIG.SPEED_BONUS.FAST; // ✅ Константы
}
```

### 4. **Улучшена типизация**

**Было:**
```typescript
const [feedback, setFeedback] = useState<{ type: 'success' | 'error' | null, message: string }>(...);
```

**Стало:**
```typescript
export interface Feedback {
  type: FeedbackType;
  message: string;
}
```

### 5. **Добавлена обработка ошибок**

```typescript
if (!currentProblem) {
  console.error('No current problem available');
  return;
}

if (!answer || answer.trim() === '') {
  setFeedback({ type: 'error', message: 'Please enter an answer' });
  return;
}
```

### 6. **Добавлена доступность (a11y)**

- `aria-label` для всех интерактивных элементов
- `aria-hidden` для декоративных элементов
- Семантические HTML теги (`<header>`, `<main>`)

---

## 🧩 Компоненты

### `MenuScreen`
- **Ответственность:** Отображение главного меню
- **Props:** `onStart: () => void`
- **Особенности:** Анимации, градиенты

### `GameScreen`
- **Ответственность:** Контейнер для игрового экрана
- **Props:** Все данные игры (profile, problem, feedback)
- **Особенности:** Композиция дочерних компонентов

### `GameHeader`
- **Ответственность:** Отображение прогресса (звезды, уровень, streak)
- **Props:** `profile: UserProfile`
- **Особенности:** HUD с иконками

### `VisualAid`
- **Ответственность:** Визуальная помощь (Ten-Frame)
- **Props:** `problem: Problem`
- **Особенности:** Интеграция с `Visualizer`

### `ProblemDisplay`
- **Ответственность:** Отображение задачи
- **Props:** `problem`, `userAnswer`
- **Особенности:** Большой шрифт, анимация

### `NumberPad`
- **Ответственность:** Цифровая клавиатура
- **Props:** `onInput`, `disabled`
- **Особенности:** Grid layout, обработка disabled состояния

---

## 🎣 Custom Hooks

### `useGameLogic`

**Ответственность:**
- Управление всем состоянием игры
- Обработка пользовательского ввода
- Взаимодействие с AI
- Управление таймерами

**Возвращает:**
```typescript
{
  gameState: GameState;
  currentProblem: Problem | null;
  userAnswer: string;
  feedback: Feedback;
  profile: UserProfile;
  startGame: () => void;
  handleInput: (value: InputValue) => void;
}
```

**Преимущества:**
- Логика отделена от UI
- Легко тестировать
- Можно переиспользовать
- Легко расширять

---

## 🔧 Конфигурация

### `constants.ts`

Все настройки в одном месте:
- `GAME_CONFIG` — настройки игры (таймеры, лимиты)
- `AI_CONFIG` — настройки AI (Elo, пороги, бонусы)
- `VISUAL_CONFIG` — цвета, анимации

**Преимущества:**
- Легко настраивать без изменения кода
- Легко A/B тестировать
- Легко документировать

---

## 📊 Метрики качества

### До рефакторинга:
- ❌ 1 монолитный компонент (205 строк)
- ❌ Глобальный экземпляр AI
- ❌ Утечки памяти
- ❌ Магические числа
- ❌ Нет обработки ошибок

### После рефакторинга:
- ✅ 8 переиспользуемых компонентов
- ✅ Custom hook для логики
- ✅ Нет утечек памяти
- ✅ Все константы вынесены
- ✅ Обработка ошибок
- ✅ Улучшенная типизация
- ✅ Доступность (a11y)

---

## 🚀 Расширяемость

### Легко добавить:
1. **Новые типы задач** — расширить `ProblemType` enum
2. **Новые экраны** — создать новый компонент и добавить в `App.tsx`
3. **Новые настройки** — добавить в `constants.ts`
4. **Новые фичи** — расширить `useGameLogic` или создать новый hook

### Пример добавления нового экрана:

```typescript
// components/ResultsScreen.tsx
export const ResultsScreen: React.FC<ResultsScreenProps> = ({ ... }) => {
  // ...
};

// App.tsx
if (gameState === GameState.RESULTS) {
  return <ResultsScreen {...resultsProps} />;
}
```

---

## 🧪 Тестируемость

### Легко тестировать:
- **Компоненты** — изолированно, с моками пропсов
- **Hooks** — через `@testing-library/react-hooks`
- **Services** — чистые функции, легко unit-тестировать
- **Константы** — статические значения

### Пример теста:

```typescript
describe('useGameLogic', () => {
  it('should start game and generate first problem', () => {
    const { result } = renderHook(() => useGameLogic());
    act(() => result.current.startGame());
    expect(result.current.currentProblem).not.toBeNull();
  });
});
```

---

## 📝 Рекомендации для дальнейшего развития

1. **State Management:**
   - Если проект вырастет — рассмотреть Zustand или Jotai
   - Пока `useState` + `useGameLogic` достаточно

2. **Роутинг:**
   - Если появятся разные режимы — React Router
   - Пока условный рендеринг в `App.tsx` работает

3. **Тестирование:**
   - Unit тесты для `NeuroMathAI`
   - Component tests для UI
   - E2E тесты для игрового процесса

4. **Производительность:**
   - `React.memo` для тяжелых компонентов
   - `useMemo` для вычислений
   - Code splitting если bundle станет большим

---

**Вердикт:** Архитектура стала чище, модульнее и легче поддерживать! 🎉

