# Functional Data Analysis (FDA) — Практическое руководство

## Что такое FDA?

**Functional Data Analysis** — это статистический подход, когда твои данные — это **кривые** (функции), а не отдельные числа.

Вместо анализа точек, FDA работает с **целыми функциями** как едиными объектами.

---

## 🧒 Пример 1: Рост детей (канонический пример)

### Данные

Измеряешь рост ребёнка в разном возрасте: 0 лет → 1 год → 2 года → … → 18 лет.

У каждого ребёнка получается **кривая роста**, не просто набор чисел.

```
Ребёнок A: быстро растёт в 12 лет (ранний пубертат)
Ребёнок B: быстро растёт в 14 лет (поздний пубертат)
```

Кривые похожи, но **сдвинуты во времени** и имеют разные «амплитуды».

### Что делает FDA?

#### 1️⃣ Сглаживание → превращает точки в непрерывную функцию

```
Сырые точки → сглаженная кривая роста: X_i(t)
```

#### 2️⃣ Находит среднюю кривую роста

Но простая средняя даёт странную плоскую линию, потому что:
- Дети проходят пубертат в разное время
- Пики «размазываются» при усреднении

#### 3️⃣ Применяет **FPCA** (Functional Principal Component Analysis)

FPCA ищет главные «моды вариации»:

- **1-я компонента** = «в целом выше/ниже среднего»
- **2-я компонента** = «ранний или поздний ростовой скачок»

Каждого ребёнка можно описать **двумя числами**, а не 50 измерениями.

#### 4️⃣ Выравнивание времени (time warping)

FDA корректирует кривые так, чтобы *события совпадали*:
- Возраст начала ускоренного роста
- Пик роста
- Окончание роста

После выравнивания средняя кривая становится очень информативной.

👉 **Это было бы невозможно в обычной статистике**, только FDA.

---

## 📡 Пример 2: Временные сигналы — аудио, датчики, IoT

### Ситуация

Запускаешь двигатель 100 раз → 100 сигналов вибрации:
- Каждый запуск длится 5 секунд
- Каждый чуть-чуть отличается

### Что делает FDA?

#### 1️⃣ Строит функциональное представление сигнала

Гладкая функция вместо дискретных точек.

#### 2️⃣ FPCA выводит ключевые паттерны

- **1-я компонента** = «общая сила вибрации»
- **2-я компонента** = «вибрация при старте»
- **3-я компонента** = «вибрация на стабилизации»

Каждый запуск преобразуется в короткий вектор из нескольких коэффициентов.

**Применение:**
- ✅ Диагностика
- ✅ Поиск аномалий
- ✅ Предиктивное обслуживание

#### 3️⃣ Time warping — нормализация времени

Если мотор прогревается быстрее или медленнее, FDA выровняет время.

**Пример:** Стартовый пик приходит в разные моменты у разных попыток → FDA подгоняет их.

---

## 🎨 Пример 3: Спектры — химия, биология, food industry

**Tecator dataset** (NIR-спектры мяса):
- Спектр (интенсивность излучения) на длинах волн 850–1050 nm
- Цель: содержание жира, белка, влаги

Спектр = **функция** от длины волны λ.

### Что делает FDA?

#### 1️⃣ Сглаживает спектр

Спектры — гладкие кривые → идеальный кандидат для FDA.

#### 2️⃣ FPCA ищет глобальные паттерны

- **1-я компонента:** общая высота спектра
- **2-я:** глубина определённых «ям» → указывают на содержание воды
- **3-я:** реакция на жир

Каждый спектр описывается 2–5 числами вместо 300+ признаков.

#### 3️⃣ Функциональная регрессия

```
Y = β₀ + ∫ X(λ) * β(λ) dλ + ε
```

Где β(λ) показывает, **какие участки спектра важны для прогноза**.

---

## 📊 FPCA в картинках (ASCII)

### Обычная PCA (для точек)

```
Точки в пространстве:
    •  •  •
  •     •     •
•         •         •

PCA находит главные направления:
    ────────────→ (1-я компонента)
```

### FPCA (для кривых)

```
Кривые:
    ╱╲     ╱╲     ╱╲
   ╱  ╲   ╱  ╲   ╱  ╲
  ╱    ╲ ╱    ╲ ╱    ╲

FPCA находит главные "формы вариации":
    ╱╲  ← 1-я компонента (высота)
   ╱  ╲
  ╱    ╲

    ╱╲  ← 2-я компонента (сдвиг во времени)
     ╱╲
      ╱╲
```

---

## 🎮 Применение в игровой аналитике

### Retention curves (кривые удержания)

**Проблема:** Игроки возвращаются в разное время, кривые retention "размазаны".

**FDA решение:**
1. Каждый игрок = кривая возвращений во времени
2. FPCA находит паттерны: "быстро уходит", "стабильный", "возвращается периодически"
3. Time warping выравнивает по ключевым событиям (первый возврат, пик активности)

### In-app behavior curves

**Проблема:** Игроки проходят уровни с разной скоростью.

**FDA решение:**
1. Кривая прогресса каждого игрока
2. FPCA выявляет: "быстрый старт", "застревает на средних уровнях", "ускоряется к концу"
3. Кластеризация по паттернам → персонализация сложности

### Связь с Midstreamer

FDA идеально дополняет Midstreamer:
- **Midstreamer:** детекция аномалий в реальном времени
- **FDA:** понимание долгосрочных паттернов поведения

**Пример:** Midstreamer видит "tilt сейчас", FDA понимает "этот игрок склонен к tilt в середине сессии".

---

## 🔬 Применение к биометрическим данным (Neuro Project)

### Биометрические сигналы как функции

В `biometricSimulator.ts` у нас есть:
- **Дыхание:** кривая интенсивности во времени
- **Сердце:** кривая BPM с HRV
- **Эмоции:** кривая stress/calm/focus

### Что может дать FDA?

#### 1️⃣ FPCA для биометрии

- **1-я компонента:** общий уровень активации
- **2-я компонента:** синхронизация дыхания и пульса
- **3-я компонента:** эмоциональные переходы

#### 2️⃣ Time warping для синхронизации

Выравнивание биометрических кривых по ключевым событиям:
- Пик стресса
- Переход в спокойствие
- Момент фокуса

#### 3️⃣ Функциональная регрессия

Предсказание эмоционального состояния по биометрическим кривым:
```
emotion(t) = β₀ + ∫ heart_rate(s) * β₁(s) ds + ∫ breath(s) * β₂(s) ds
```

### Интеграция с emotional-core

FDA может анализировать **кривые эмоций** из `emotional-core.ts`:
- Паттерны "anticipation → trust → focus"
- Аномалии в эмоциональных переходах
- Кластеризация по эмоциональным профилям

---

## 💻 FDA в коде (Python пример)

```python
import numpy as np
from scipy.interpolate import interp1d
from sklearn.decomposition import PCA

# 1. Сглаживание: превращаем точки в функцию
def smooth_curve(points, times):
    """Преобразует дискретные точки в гладкую функцию"""
    f = interp1d(times, points, kind='cubic', fill_value='extrapolate')
    return f

# 2. FPCA (упрощённая версия)
def functional_pca(curves, n_components=3):
    """
    curves: список функций (кривых)
    Возвращает: главные компоненты и коэффициенты
    """
    # Дискретизируем кривые на общую сетку
    t_grid = np.linspace(0, 1, 100)
    data_matrix = np.array([curve(t_grid) for curve in curves])
    
    # Применяем обычную PCA
    pca = PCA(n_components=n_components)
    components = pca.fit_transform(data_matrix)
    
    return pca, components

# 3. Time warping (упрощённая версия)
def align_curves(curves, reference_events):
    """
    Выравнивает кривые по ключевым событиям
    reference_events: список моментов времени для выравнивания
    """
    aligned = []
    for curve in curves:
        # Находим события в текущей кривой
        events = find_events(curve)
        # Вычисляем warping function
        warp = compute_warping(events, reference_events)
        # Применяем warping
        aligned.append(apply_warping(curve, warp))
    return aligned
```

---

## 📈 Сравнение: FDA vs обычная ML-PCA

| Аспект | Обычная PCA | FDA (FPCA) |
|--------|-------------|------------|
| **Данные** | Точки в пространстве | Кривые (функции) |
| **Размерность** | Фиксированная | Бесконечная (сводится к конечной) |
| **Время** | Не учитывается | Учитывается (time warping) |
| **Сглаживание** | Не требуется | Обязательно |
| **Интерпретация** | Направления в пространстве | Паттерны вариации кривых |
| **Применение** | Табличные данные | Временные ряды, спектры, траектории |

---

## 🎯 Короткое резюме

FDA = когда твои данные — **кривые**, а не числа.

**FDA позволяет:**
- ✅ Сглаживать кривые
- ✅ Выравнивать по времени (time warping)
- ✅ Находить скрытые паттерны (FPCA)
- ✅ Делать регрессию «кривая → число/кривая»
- ✅ Кластеризовать кривые
- ✅ Исправлять сдвиги и растяжения во времени

---

## 🔗 Связь с Neuro Project

### Биометрия
- Кривые дыхания, пульса, эмоций → FPCA для выявления паттернов
- Time warping для синхронизации биометрических сигналов

### Игровая аналитика
- Retention curves → кластеризация игроков
- Behavior curves → персонализация сложности

### Swarm impulses
- Кривые распространения импульсов → анализ паттернов роя
- Эмоциональные траектории → понимание эволюции состояний

### Midstreamer integration
- FDA для долгосрочных паттернов
- Midstreamer для реального времени
- Вместе = полная картина поведения

---

## 📚 Следующие шаги

1. **Реализация FPCA** для биометрических данных
2. **Time warping** для синхронизации сигналов
3. **Интеграция с Midstreamer** для гибридного анализа
4. **Визуализация** паттернов в Three.js (Neuro визуализация)

---

## Ссылки

- [FDA в R: fda package](https://cran.r-project.org/web/packages/fda/index.html)
- [FDA в Python: scikit-fda](https://fda.readthedocs.io/)
- [Tecator dataset](https://www.math.univ-toulouse.fr/~ferraty/SOFTWARES/NPFDA/npfda-datasets.html)

