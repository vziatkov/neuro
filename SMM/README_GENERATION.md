# 🎬 Neural Network Video Generation

---

## 🇬🇧 English

### ✅ Ready Files

#### Extended 10-second version
- **`neuro_processed_10s.mp4`** (9.2MB) — 10-second version with cyclic repeat of last 2 seconds

#### Source files
- **`neuro_processed.mp4`** (4.3MB) — processed video with effects
- **`neuro_teaser.gif`** (3.2MB) — GIF teaser

### 🎨 Generating New Video from Prompt

#### Option 1: Using Generative Services (Recommended)

Open the **`VEO_PROMPT.md`** file — there are ready prompts for:
- Google Veo
- Runway Gen-2
- Pika Labs
- Other generative video services

**Quick Start:**
1. Open `VEO_PROMPT.md`
2. Copy the prompt (extended version recommended)
3. Paste into generative service
4. Set parameters: 10 sec, 16:9, 24 fps

#### Option 2: Three.js Generation (Local)

```bash
# Run generation script
python3 generate_neural_video.py

# HTML file with visualization will open
# For video recording use:
# - OBS Studio (screen recording)
# - Puppeteer + ffmpeg (automation)
```

#### Option 3: Using Neuro Project

If you have access to the Neuro project, you can:
1. Run visualization
2. Use desired formation (e.g., "Quantum Cortex" or "Neural Vortex")
3. Record video via OBS or built-in export

### 📝 Effect Description from Prompt

- **Dark cosmic background** — deep black/dark blue gradient
- **Glowing 3D wireframe** — nodes and connections with glow effect
- **Colors**: purple (#8b5cf6), blue (#3b82f6), turquoise (#4fd1c5)
- **Breathing** — smooth expansion/contraction (cycle ~6 seconds)
- **Shape**: flower or spiral
- **Camera**: slow rotation around structure
- **Atmosphere**: meditative, futuristic

### 🔧 Technical Details

#### Creating 10-second version with loop

```bash
# Using ffmpeg
ffmpeg -i neuro_processed.mp4 \
  -filter_complex "[0:v]trim=start=0:end=7.8,setpts=PTS-STARTPTS[main]; \
                   [0:v]trim=start=5.8:end=7.8,setpts=PTS-STARTPTS,loop=loop=-1:size=48[loop]; \
                   [main][loop]concat=n=2:v=1[out]" \
  -map "[out]" -t 10 -c:v libx264 -preset slow -crf 18 \
  -y neuro_processed_10s.mp4
```

#### Optimization

```bash
# Optimize ready video
python3 optimize_graphics.py neuro_processed_10s.mp4
```

### 📦 Dependencies

- `ffmpeg` — for video work
- `python3` — for scripts
- `PIL/Pillow` — for image processing
- `moviepy` — for video work (optional)

### 💡 Tips

1. **For presentation**: use `neuro_processed_10s.mp4` — it's ready
2. **For generating new**: use prompts from `VEO_PROMPT.md`
3. **For customization**: edit `process_video.py` to change effects
4. **For optimization**: use `optimize_graphics.py` for all files

**Done!** All files are in the `SMM/` folder

---

## 🇷🇺 Русский

### ✅ Готовые файлы

#### Расширенная 10-секундная версия
- **`neuro_processed_10s.mp4`** (9.2MB) — 10-секундная версия с циклическим повтором последних 2 секунд

#### Исходные файлы
- **`neuro_processed.mp4`** (4.3MB) — обработанное видео с эффектами
- **`neuro_teaser.gif`** (3.2MB) — GIF-тизер

### 🎨 Генерация нового видео по промту

#### Вариант 1: Использование генеративных сервисов (рекомендуется)

Откройте файл **`VEO_PROMPT.md`** — там готовые промты для:
- Google Veo
- Runway Gen-2
- Pika Labs
- Другие генеративные видео-сервисы

**Быстрый старт:**
1. Откройте `VEO_PROMPT.md`
2. Скопируйте промт (рекомендуется расширенный вариант)
3. Вставьте в генеративный сервис
4. Установите параметры: 10 сек, 16:9, 24 fps

#### Вариант 2: Three.js генерация (локально)

```bash
# Запустить скрипт генерации
python3 generate_neural_video.py

# Откроется HTML-файл с визуализацией
# Для записи видео используйте:
# - OBS Studio (экранная запись)
# - Puppeteer + ffmpeg (автоматизация)
```

#### Вариант 3: Использование проекта Neuro

Если у вас есть доступ к проекту Neuro, можно:
1. Запустить визуализацию
2. Использовать нужную формацию (например, "Quantum Cortex" или "Neural Vortex")
3. Записать видео через OBS или встроенный экспорт

### 📝 Описание эффектов из промта

- **Темный космический фон** — глубокий черный/темно-синий градиент
- **Светящийся 3D-каркас** — узлы и связи с glow-эффектом
- **Цвета**: фиолетовый (#8b5cf6), синий (#3b82f6), бирюзовый (#4fd1c5)
- **Дыхание** — плавное расширение/сжатие (цикл ~6 секунд)
- **Форма**: цветок или спираль
- **Камера**: медленное вращение вокруг структуры
- **Атмосфера**: медитативная, футуристичная

### 🔧 Технические детали

#### Создание 10-секундной версии с циклом

```bash
# Используя ffmpeg
ffmpeg -i neuro_processed.mp4 \
  -filter_complex "[0:v]trim=start=0:end=7.8,setpts=PTS-STARTPTS[main]; \
                   [0:v]trim=start=5.8:end=7.8,setpts=PTS-STARTPTS,loop=loop=-1:size=48[loop]; \
                   [main][loop]concat=n=2:v=1[out]" \
  -map "[out]" -t 10 -c:v libx264 -preset slow -crf 18 \
  -y neuro_processed_10s.mp4
```

#### Оптимизация

```bash
# Оптимизация готового видео
python3 optimize_graphics.py neuro_processed_10s.mp4
```

### 📦 Зависимости

- `ffmpeg` — для работы с видео
- `python3` — для скриптов
- `PIL/Pillow` — для обработки изображений
- `moviepy` — для работы с видео (опционально)

### 💡 Советы

1. **Для презентации**: используйте `neuro_processed_10s.mp4` — он уже готов
2. **Для генерации нового**: используйте промты из `VEO_PROMPT.md`
3. **Для кастомизации**: редактируйте `process_video.py` для изменения эффектов
4. **Для оптимизации**: используйте `optimize_graphics.py` для всех файлов

**Готово!** Все файлы находятся в папке `SMM/`

---

## 🇺🇦 Українська

### ✅ Готові файли

#### Розширена 10-секундна версія
- **`neuro_processed_10s.mp4`** (9.2MB) — 10-секундна версія з циклічним повтором останніх 2 секунд

#### Вихідні файли
- **`neuro_processed.mp4`** (4.3MB) — оброблене відео з ефектами
- **`neuro_teaser.gif`** (3.2MB) — GIF-тізер

### 🎨 Генерація нового відео за промптом

#### Варіант 1: Використання генеративних сервісів (рекомендовано)

Відкрийте файл **`VEO_PROMPT.md`** — там готові промпти для:
- Google Veo
- Runway Gen-2
- Pika Labs
- Інші генеративні відео-сервіси

**Швидкий старт:**
1. Відкрийте `VEO_PROMPT.md`
2. Скопіюйте промпт (рекомендовано розширений варіант)
3. Вставте в генеративний сервіс
4. Встановіть параметри: 10 сек, 16:9, 24 fps

#### Варіант 2: Three.js генерація (локально)

```bash
# Запустити скрипт генерації
python3 generate_neural_video.py

# Відкриється HTML-файл з візуалізацією
# Для запису відео використовуйте:
# - OBS Studio (екранний запис)
# - Puppeteer + ffmpeg (автоматизація)
```

#### Варіант 3: Використання проекту Neuro

Якщо у вас є доступ до проекту Neuro, можна:
1. Запустити візуалізацію
2. Використати потрібну формацію (наприклад, "Quantum Cortex" або "Neural Vortex")
3. Записати відео через OBS або вбудований експорт

### 📝 Опис ефектів з промпту

- **Темний космічний фон** — глибокий чорний/темно-синій градієнт
- **Світний 3D-каркас** — вузли та зв'язки з glow-ефектом
- **Кольори**: фіолетовий (#8b5cf6), синій (#3b82f6), бірюзовий (#4fd1c5)
- **Дихання** — плавне розширення/стиснення (цикл ~6 секунд)
- **Форма**: квітка або спіраль
- **Камера**: повільне обертання навколо структури
- **Атмосфера**: медитативна, футуристична

### 🔧 Технічні деталі

#### Створення 10-секундної версії з циклом

```bash
# Використовуючи ffmpeg
ffmpeg -i neuro_processed.mp4 \
  -filter_complex "[0:v]trim=start=0:end=7.8,setpts=PTS-STARTPTS[main]; \
                   [0:v]trim=start=5.8:end=7.8,setpts=PTS-STARTPTS,loop=loop=-1:size=48[loop]; \
                   [main][loop]concat=n=2:v=1[out]" \
  -map "[out]" -t 10 -c:v libx264 -preset slow -crf 18 \
  -y neuro_processed_10s.mp4
```

#### Оптимізація

```bash
# Оптимізація готового відео
python3 optimize_graphics.py neuro_processed_10s.mp4
```

### 📦 Залежності

- `ffmpeg` — для роботи з відео
- `python3` — для скриптів
- `PIL/Pillow` — для обробки зображень
- `moviepy` — для роботи з відео (опціонально)

### 💡 Поради

1. **Для презентації**: використовуйте `neuro_processed_10s.mp4` — він уже готовий
2. **Для генерації нового**: використовуйте промпти з `VEO_PROMPT.md`
3. **Для кастомізації**: редагуйте `process_video.py` для зміни ефектів
4. **Для оптимізації**: використовуйте `optimize_graphics.py` для всіх файлів

**Готово!** Всі файли знаходяться в папці `SMM/`

---
