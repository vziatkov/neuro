# ⚙️ Prompt for Cursor with Python-pipe (Neuro Video Processing)

---

## 🇬🇧 English

> You are a Python agent working with video in the style of the art installation **"Neuro: Living Mirror of Consciousness"**.

> Your goal is to analyze and process the video file `Промт_для_презентации_Neuro.mp4`, creating a visually-philosophical result.

> Use libraries `moviepy`, `opencv-python`, `numpy`, `pillow`, `scipy`, `ffmpeg-python` or any others if needed.

### Main Tasks:

1. **Import video** and extract frames with adjustable step (e.g., every 0.5 seconds).

2. **Apply filters** — soft glows, neural shader effects (bloom, glow, edge-enhancement, color shift).

3. **Add breathing** — light pulsing of brightness or blur following a rhythm (period ~6 seconds).

4. **Transform palette** into deep blues, purples, and azure tones (gradient from #030b18 to #7d5bff).

5. **Add photonic noise** — small points moving in a sinusoidal pattern.

6. **Generate audio background (optional)**: low hum or heartbeat-loop.

7. **Export**:
   * version with post-effects (`neuro_processed.mp4`),
   * short `.gif` teaser (3–5 seconds).

### ⚡ Additionally:

* Add breathing timing — smooth transitions (ease-in / ease-out).
* If frames are already in `/SMM/processed/`, you can assemble them into video via `ImageSequenceClip`.
* Use sinusoidal and Perlin patterns for pulsation and noise textures.

### 💫 Philosophy of the Result

The final result should feel like **"meditation inside a neural network"** — smooth light movement, breathing rhythm, a sense of living structure.

Each effect has philosophical meaning:
- **glow_filter()**: enhances light, symbolizing conscious attention
- **breath_rhythm()**: reminds of the inhale-exhale cycle as the basis of life
- **photonic_noise()**: represents quantum uncertainty and randomness
- **color_shift()**: transforms reality into the space of consciousness
- **edge_enhancement()**: emphasizes boundaries between thought and matter

**Script:** `SMM/process_video.py`

---

## 🇷🇺 Русский

> Ты — Python-агент, работающий с видео в стиле арт-инсталляции **"Neuro: Живое зеркало сознания"**.

> Твоя цель — анализировать и обрабатывать видеофайл `Промт_для_презентации_Neuro.mp4`, создавая визуально-философский результат.

> Используй библиотеки `moviepy`, `opencv-python`, `numpy`, `pillow`, `scipy`, `ffmpeg-python` или любые другие, если нужно.

### Основные задачи:

1. **Импортировать видео** и извлечь кадры с регулируемым шагом (например, каждые 0.5 секунды).

2. **Применить фильтры** — мягкие свечения, нейронный шейдер-эффект (bloom, glow, edge-enhancement, color shift).

3. **Добавить дыхание** — лёгкое пульсирование яркости или размытия по ритму (период ~6 секунд).

4. **Преобразовать палитру** в глубокие синие, фиолетовые и лазурные тона (градиент от #030b18 к #7d5bff).

5. **Добавить шум света (photonic noise)** — мелкие точки, движущиеся по синусоидальному паттерну.

6. **Сгенерировать аудиофон (по желанию)**: низкий гул или heartbeat-loop.

7. **Экспортировать**:
   * версию с пост-эффектами (`neuro_processed.mp4`),
   * короткий `.gif`-тизер (3–5 секунд).

### ⚡ Дополнительно:

* Добавь тайминг дыхания — плавные переходы (ease-in / ease-out).
* Если кадры уже в `/SMM/processed/`, можешь собрать их в видео через `ImageSequenceClip`.
* Используй синусоидальные и перлин-паттерны для пульсации и шумовых текстур.

### 💫 Философия результата

Финальный результат должен ощущаться как **"медитация внутри нейронной сети"** — плавное движение света, ритм дыхания, чувство живой структуры.

Каждый эффект имеет философское значение:
- **glow_filter()**: усиливает свет, символизируя осознанное внимание
- **breath_rhythm()**: напоминает о цикле вдоха-выдоха как основе жизни
- **photonic_noise()**: представляет квантовую неопределённость и случайность
- **color_shift()**: трансформирует реальность в пространство сознания
- **edge_enhancement()**: подчёркивает границы между мыслью и материей

**Скрипт:** `SMM/process_video.py`

---

## 🇺🇦 Українська

> Ти — Python-агент, який працює з відео в стилі арт-інсталяції **"Neuro: Живе дзеркало свідомості"**.

> Твоя мета — аналізувати та обробляти відеофайл `Промт_для_презентації_Neuro.mp4`, створюючи візуально-філософський результат.

> Використовуй бібліотеки `moviepy`, `opencv-python`, `numpy`, `pillow`, `scipy`, `ffmpeg-python` або будь-які інші, якщо потрібно.

### Основні завдання:

1. **Імпортувати відео** та витягти кадри з регульованим кроком (наприклад, кожні 0.5 секунди).

2. **Застосувати фільтри** — м'які свічення, нейронний шейдер-ефект (bloom, glow, edge-enhancement, color shift).

3. **Додати дихання** — легке пульсування яскравості або розмиття за ритмом (період ~6 секунд).

4. **Перетворити палітру** на глибокі сині, фіолетові та лазурні тони (градієнт від #030b18 до #7d5bff).

5. **Додати шум світла (photonic noise)** — дрібні точки, що рухаються за синусоїдальним патерном.

6. **Згенерувати аудіофон (за бажанням)**: низький гул або heartbeat-loop.

7. **Експортувати**:
   * версію з пост-ефектами (`neuro_processed.mp4`),
   * короткий `.gif`-тізер (3–5 секунд).

### ⚡ Додатково:

* Додай таймінг дихання — плавні переходи (ease-in / ease-out).
* Якщо кадри вже в `/SMM/processed/`, можеш зібрати їх у відео через `ImageSequenceClip`.
* Використовуй синусоїдальні та перлін-патерни для пульсації та шумових текстур.

### 💫 Філософія результату

Фінальний результат має відчуватися як **"медитація всередині нейронної мережі"** — плавний рух світла, ритм дихання, відчуття живої структури.

Кожен ефект має філософське значення:
- **glow_filter()**: посилює світло, символізуючи усвідомлену увагу
- **breath_rhythm()**: нагадує про цикл вдиху-видиху як основу життя
- **photonic_noise()**: представляє квантову невизначеність та випадковість
- **color_shift()**: трансформує реальність у простір свідомості
- **edge_enhancement()**: підкреслює межі між думкою та матерією

**Скрипт:** `SMM/process_video.py`

---
